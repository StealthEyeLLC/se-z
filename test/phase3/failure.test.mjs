import test from 'node:test';import assert from 'node:assert/strict';import {mkdtempSync} from 'node:fs';import {tmpdir} from 'node:os';import {join} from 'node:path';import {GatewayState} from '../../src/phase3/atomic-state.mjs';
test('clock forward expires refresh and corrupt state fails closed',()=>{const now={value:1000},s=new GatewayState(mkdtempSync(join(tmpdir(),'p3-fail-')),{hashSecret:Buffer.alloc(32,2),now:()=>now.value}),r=s.issueRefresh('blue',{clientId:'c',subject:'s',githubId:1,resource:'r',scope:['sez.root'],absoluteExpiry:2000,idleExpiry:500});now.value=3000;assert.throws(()=>s.rotateRefresh('blue',r.raw,{clientId:'c',resource:'r'}),/oauth_invalid_grant/);});
