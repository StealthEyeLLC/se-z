# artifacts parity

Executable source-derived tests for this family are enumerated in `test/parity/fixtures/family-matrix.json` and run by `phase1:parity`. Dynamic identifiers are normalized without discarding bytes, offsets, modes, exit status, signals, digests, verification results, or cleanup state.
