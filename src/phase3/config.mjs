import {PHASE3} from './constants.mjs';
const allowed=new Set(['issuer','protectedResource','authorizationOrigin','mcpHost','mcpPort','stateRoot','activeSlot','gatewayStateSchema','gatewayId','gatewayKeyId','gatewaySigningCredential','receiptVerificationKeys','oauthSigningCredential','oauthAcceptedKeys','refreshHashCredential','githubOauthClientIdCredential','githubOauthClientSecretCredential','githubCallbackUri','ownerGithubId','accessTokenTtlSeconds','authorizationCodeTtlSeconds','authorizationStateTtlSeconds','refreshAbsoluteTtlSeconds','refreshIdleTtlSeconds','requestBodyLimit','tunnelProfileReference','tunnelCredentialReference','supervisorGatewaySocket','releaseIdentity','testMode']);
export function validateGatewayConfig(input,{production=true}={}){if(!input||typeof input!=='object'||Array.isArray(input))throw new Error('configuration must be an object');for(const k of Object.keys(input))if(!allowed.has(k))throw new Error(`unknown configuration field: ${k}`);const required=['issuer','protectedResource','authorizationOrigin','mcpHost','mcpPort','stateRoot','gatewayStateSchema','gatewayId','gatewayKeyId','gatewaySigningCredential','receiptVerificationKeys','oauthSigningCredential','oauthAcceptedKeys','refreshHashCredential','githubOauthClientIdCredential','githubOauthClientSecretCredential','githubCallbackUri','ownerGithubId','supervisorGatewaySocket','releaseIdentity'];for(const k of required)if(input[k]===undefined||input[k]==='')throw new Error(`missing configuration field: ${k}`);if(input.issuer!==PHASE3.issuer||input.authorizationOrigin!==PHASE3.issuer)throw new Error('issuer identity mismatch');const resource=new URL(input.protectedResource);if(resource.protocol!=='https:'||resource.href!==input.protectedResource)throw new Error('protected resource must be exact canonical URL');if(!['127.0.0.1','::1'].includes(input.mcpHost))throw new Error('MCP listener must be loopback-only');if(input.gatewayStateSchema!==PHASE3.stateSchema||input.ownerGithubId!==PHASE3.ownerGithubId)throw new Error('canonical identity mismatch');if(input.supervisorGatewaySocket!==PHASE3.gatewaySocket)throw new Error('gateway socket mismatch');if(production&&input.testMode)throw new Error('test bypass forbidden in production configuration');return Object.freeze(structuredClone(input));}
