# PHASE3 GATEWAY STATE

Gateway state uses isolated blue and green stores under /var/lib/se-z-gateway, atomic fsync/rename writes, inactive-copy migration, digest manifests, restore rejection, and monotonic reconciliation of consumed credentials.
