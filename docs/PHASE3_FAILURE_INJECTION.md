# PHASE3 FAILURE INJECTION

Executed local failures include malformed input, wrong redirect and resource, PKCE failure, wrong GitHub ID, code replay, refresh replay, revocation, clock forward, corrupt snapshot, public bind rejection, wrong host, response loss, receipt integrity checks, and production recovery unavailability.
