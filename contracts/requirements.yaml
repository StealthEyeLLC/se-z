version: 1
product: se-z
requirements:
  - id: INV-PUBLIC-001
    class: invariant
    statement: The public MCP catalog exposes exactly one tool named call_sez.
    enforcedBy: [gateway]
    tests: [public_catalog_exactly_one_tool]
    evidence: [public-catalog.json]
    releaseGate: mandatory
  - id: INV-PUBLIC-002
    class: invariant
    statement: The call_sez schema contains only operation, payload, and idempotencyKey and never enumerates operations.
    enforcedBy: [gateway, ci]
    tests: [public_tool_schema_frozen]
    evidence: [tool-schema.json]
    releaseGate: mandatory
  - id: INV-FAST-001
    class: invariant
    statement: Missing lane, target, user, group, and cwd resolve to fast, host, root, root, and /root.
    enforcedBy: [supervisor]
    tests: [fast_lane_defaults]
    evidence: [fast-default-result.json]
    releaseGate: mandatory
  - id: INV-SEZ1-001
    class: invariant
    statement: Every SEZ1 request contains authorityGeneration and the canonical request digest binds it.
    enforcedBy: [gateway, cli, supervisor]
    tests: [authority_generation_in_schema, authority_generation_digest_vector]
    evidence: [sez1-test-vectors.json]
    releaseGate: mandatory
  - id: INV-SEZ1-002
    class: invariant
    statement: Every SEZ1 response contains catalogDigest.
    enforcedBy: [supervisor, gateway]
    tests: [catalog_digest_every_response]
    evidence: [response-samples.json]
    releaseGate: mandatory
  - id: INV-STREAM-001
    class: invariant
    statement: Process output is captured in durable streams and complete output remains retrievable beyond the 16 MiB frame limit.
    enforcedBy: [supervisor, job-store]
    tests: [output_over_frame_limit_round_trip]
    evidence: [large-stream-proof.json]
    releaseGate: mandatory
  - id: INV-CALLER-001
    class: invariant
    statement: Catalog caching and automatic wait behavior are caller guidance; deterministic wait and resume operations exist on the server.
    enforcedBy: [catalog, job-controller]
    tests: [request_resume_after_response_loss, deterministic_job_wait]
    evidence: [resume-proof.json]
    releaseGate: mandatory
  - id: INV-OAUTH-001
    class: invariant
    statement: The tunnel-backed resource publishes protected-resource metadata and returns a WWW-Authenticate challenge with the exact resource metadata URL.
    enforcedBy: [gateway]
    tests: [protected_resource_metadata, unauthorized_challenge]
    evidence: [oauth-discovery.json, unauthorized-response.txt]
    releaseGate: mandatory
  - id: INV-OAUTH-002
    class: invariant
    statement: The exact protected resource is propagated through authorization and token requests and validated as access-token audience.
    enforcedBy: [gateway-oauth]
    tests: [resource_parameter_authorization, resource_parameter_token, audience_mismatch_rejected]
    evidence: [oauth-resource-binding.json]
    releaseGate: mandatory
  - id: INV-OAUTH-003
    class: invariant
    statement: sez.root is the only authority scope; offline_access is advertised solely for refresh-token lifecycle support.
    enforcedBy: [gateway-oauth]
    tests: [scope_metadata, refresh_rotation, refresh_replay_rejected]
    evidence: [oauth-scope-proof.json]
    releaseGate: mandatory
  - id: INV-OAUTH-004
    class: invariant
    statement: Gateway OAuth state is stored under /var/lib/se-z-gateway with isolated blue and green state slots and is included in restore tests.
    enforcedBy: [gateway, recovery, backup]
    tests: [gateway_state_slot_isolation, gateway_state_restore]
    evidence: [gateway-state-layout.json, gateway-restore-proof.json]
    releaseGate: mandatory
  - id: INV-SOCKET-001
    class: invariant
    statement: Gateway and local operators use separate sockets and peer classes.
    enforcedBy: [systemd, supervisor]
    tests: [gateway_cannot_use_local_socket, local_peer_cannot_impersonate_gateway]
    evidence: [socket-peer-proof.json]
    releaseGate: mandatory
  - id: INV-SOCKET-002
    class: invariant
    statement: Every gateway-socket request requires both the configured gateway SO_PEERCRED identity and a valid signed envelope.
    enforcedBy: [supervisor]
    tests: [unsigned_gateway_request_rejected, wrong_gateway_uid_rejected]
    evidence: [gateway-auth-proof.json]
    releaseGate: mandatory
  - id: INV-STATE-001
    class: invariant
    statement: Gateway, supervisor, z, and recovery state ownership never overlaps as competing truth.
    enforcedBy: [architecture, supervisor, recovery]
    tests: [state_owner_contract]
    evidence: [state-owner-readback.json]
    releaseGate: mandatory
  - id: INV-FENCE-001
    class: invariant
    statement: Authority generation advances only after physical old-supervisor and descendant fencing or explicit job adoption.
    enforcedBy: [recovery, systemd]
    tests: [old_cgroup_empty_before_handoff, stale_generation_rejected, adopted_job_identity]
    evidence: [fencing-evidence.json]
    releaseGate: mandatory
  - id: INV-RECOVERY-001
    class: invariant
    statement: Recovery is separately versioned and ordinary releases cannot modify its executable or state schema.
    enforcedBy: [recovery, filesystem-layout, release-controller]
    tests: [ordinary_release_cannot_replace_recovery, recovery_blue_green_upgrade]
    evidence: [recovery-independence.json]
    releaseGate: mandatory
  - id: INV-TARGET-001
    class: invariant
    statement: host, nspawn, and kvm targets never silently fall back to another target or acceleration mode.
    enforcedBy: [supervisor, machine-adapters]
    tests: [no_target_fallback, no_tcg_fallback]
    evidence: [target-failure-proof.json]
    releaseGate: mandatory
  - id: INV-KVM-001
    class: invariant
    statement: Every release bundles an exact digest-pinned z and KVM is claimed only after the full real-hardware lifecycle passes.
    enforcedBy: [release-manifest, kvm-adapter]
    tests: [bundled_z_identity, real_kvm_lifecycle]
    evidence: [z-identity.json, kvm-acceptance.json]
    releaseGate: mandatory
  - id: INV-SECRET-001
    class: invariant
    statement: Managed credentials are never automatically injected or exposed, while explicit root-authorized output may contain sensitive owner data.
    enforcedBy: [gateway, supervisor, evidence-store]
    tests: [managed_credentials_not_auto_injected, raw_owner_read_remains_possible]
    evidence: [credential-handling-proof.json]
    releaseGate: mandatory
  - id: INV-MIGRATION-001
    class: invariant
    statement: Standalone cutover removes Baby runtime dependencies but preserves legitimate provenance and historical evidence.
    enforcedBy: [build, dependency-scan]
    tests: [zero_baby_runtime_dependency]
    evidence: [dependency-scan.json]
    releaseGate: mandatory
  - id: ANTI-POLICY-001
    class: anti-invariant
    statement: No se-z command allowlist, path allowlist, policy engine, or se-z approval layer exists in the fast lane.
    enforcedBy: [architecture, ci]
    tests: [raw_exec_unrestricted_contract]
    evidence: [fast-lane-contract.json]
    releaseGate: mandatory
  - id: ANTI-PUBLIC-MCP-001
    class: anti-invariant
    statement: No public MCP ingress is active in the owner-only standalone profile.
    enforcedBy: [caddy, firewall, tunnel]
    tests: [public_mcp_route_absent, tunnel_mcp_available]
    evidence: [network-surface.json]
    releaseGate: mandatory
  - id: ANTI-SUCCESS-001
    class: anti-invariant
    statement: Accepted or running work is never reported as completed.
    enforcedBy: [job-controller, gateway]
    tests: [running_job_not_success]
    evidence: [job-state-proof.json]
    releaseGate: mandatory
  - id: ANTI-SPLIT-001
    class: anti-invariant
    statement: Two supervisor generations never mutate shared state concurrently.
    enforcedBy: [recovery, supervisor]
    tests: [candidate_active_mutation_race]
    evidence: [split-brain-proof.json]
    releaseGate: mandatory
  - id: ANTI-THEATER-001
    class: anti-invariant
    statement: Controls without an enforcement effect, acceptance test, or recovery value are not added.
    enforcedBy: [canonical-review]
    tests: [requirements_have_enforcement_and_evidence]
    evidence: [requirements-audit.json]
    releaseGate: mandatory

  - id: INV-PHASE1-SOURCE-001
    class: invariant
    statement: Exact deployed Baby supervisor and gateway commits, trees, manifests, and source archives are pinned before extraction.
    enforcedBy: [source-identity, materialize-source, phase1-verify]
    tests: [source_identity_exact_commits_and_trees, pinned_source_materialization]
    evidence: [evidence/phase1/source-identity-proof.json, evidence/phase1/source-materialization.json]
    releaseGate: mandatory
  - id: INV-PHASE1-PROVENANCE-001
    class: invariant
    statement: Every copied or substantially derived file has an exact source mapping and applicable notice classification.
    enforcedBy: [provenance-map, phase1-verify]
    tests: [provenance_complete_for_every_destination]
    evidence: [vendor/baby-provenance/file-map.json, vendor/baby-provenance/license-map.json]
    releaseGate: mandatory
  - id: INV-PHASE1-PARITY-001
    class: invariant
    statement: Pinned source suites and mechanically ported target suites measure retained behavior, with every discrepancy explicitly classified.
    enforcedBy: [source-test, extracted-test, parity, canonical-delta]
    tests: [phase1_static_parity, phase1_behavioral_parity]
    evidence: [evidence/phase1/source-test-results.json, evidence/phase1/extracted-test-results.json, evidence/phase1/parity-results.json]
    releaseGate: mandatory
  - id: INV-PHASE1-CAPABILITY-001
    class: invariant
    statement: Every installed source operation and meaningful internal capability is represented in the Phase 1 capability map; none disappears silently.
    enforcedBy: [capability-map, phase1-verify]
    tests: [capability_map_all_installed_operations, capability_map_no_silent_disappearances]
    evidence: [contracts/phase1-capability-map.json]
    releaseGate: mandatory
  - id: INV-PHASE1-IDENTITY-001
    class: invariant
    statement: Active target runtime identity is se-z, call_sez, sez.*, and SEZ1; historical Baby identities remain only in allowed provenance, tests, and evidence.
    enforcedBy: [dependency-scan, canonical-check]
    tests: [active_identity_is_se_z, historical_identity_exemptions_are_bounded]
    evidence: [evidence/phase1/dependency-scan.json]
    releaseGate: mandatory
  - id: INV-PHASE1-RUNTIME-DEPENDENCY-001
    class: invariant
    statement: Extracted target runtime code intentionally depends on no Baby checkout, service, socket, state, key, endpoint, public tool, operation namespace, or QRT1 product identity.
    enforcedBy: [dependency-scan, phase1-verify]
    tests: [zero_active_baby_runtime_dependency]
    evidence: [evidence/phase1/dependency-scan.json]
    releaseGate: mandatory
  - id: INV-PHASE1-PRODUCTION-001
    class: invariant
    statement: Phase 1 leaves deployed Baby services, release pointers, socket, manifests, OAuth configuration, credentials, public endpoint, and ChatGPT app routing unchanged; routine authorized job, stream, and receipt records may accrue.
    enforcedBy: [read-only-production-proof, phase1-report]
    tests: [production_before_after_identity_equal]
    evidence: [evidence/phase1/source-identity-proof.json, evidence/phase1/production-readback.json, evidence/phase1/phase1-summary.json]
    releaseGate: mandatory
  - id: ANTI-PHASE1-REFACTOR-001
    class: anti-invariant
    statement: Proven implementation mechanics are not substantively redesigned before source pinning, provenance, build, tests, and measured parity.
    enforcedBy: [commit-order, provenance-map, canonical-delta]
    tests: [mechanical_extraction_precedes_refactor]
    evidence: [docs/PROVENANCE.md, docs/PHASE1_CANONICAL_DELTA.md]
    releaseGate: mandatory
  - id: ANTI-PHASE1-SECRET-001
    class: anti-invariant
    statement: No production private key, token, OAuth secret, tunnel credential, backup credential, live environment file, or arbitrary credential payload enters the repository.
    enforcedBy: [secret-scan, git-review]
    tests: [phase1_repository_secret_scan]
    evidence: [evidence/phase1/secret-scan.json]
    releaseGate: mandatory
  - id: ANTI-PHASE1-CLAIM-001
    class: anti-invariant
    statement: Phase 1 is never represented as standalone completion or as activation of call_sez, final SEZ1, nspawn, KVM, tunnel, recovery, or production cutover.
    enforcedBy: [phase1-report, documentation-consistency]
    tests: [phase1_claim_is_bounded]
    evidence: [docs/PHASE1_REPORT.md, evidence/phase1/phase1-summary.json]
    releaseGate: mandatory

  # Phase 2 — Kernel — release milestone 0.1A
  - id: INV-P2-SEZ1-001
    class: invariant
    statement: Active transport uses four-byte big-endian length framing followed by strict canonical UTF-8 JSON with a 16 MiB frame maximum.
    enforcedBy: [src/kernel/protocol.mjs]
    tests: [phase2_protocol_framing]
    evidence: [evidence/phase2/protocol-results.json]
    releaseGate: mandatory
  - id: INV-P2-SEZ1-002
    class: invariant
    statement: Every accepted operation response binds request, result, authority generation, and complete active catalog digest through a signed SEZ1 receipt.
    enforcedBy: [src/kernel/operations.mjs]
    tests: [phase2_response_and_receipt_contract]
    evidence: [evidence/phase2/receipt-results.json]
    releaseGate: mandatory
  - id: INV-P2-GENERATION-001
    class: invariant
    statement: The supervisor reads recovery-owned authority generation and rejects stale or future requests before request reservation or mutation.
    enforcedBy: [src/kernel/identity.mjs, src/kernel/operations.mjs]
    tests: [phase2_generation_pre_dispatch]
    evidence: [evidence/phase2/generation-results.json]
    releaseGate: mandatory
  - id: INV-P2-SOCKET-001
    class: invariant
    statement: Gateway and local peer classes are accepted on distinct real Unix sockets whose listener identity is not taken from the envelope.
    enforcedBy: [src/kernel/server.mjs, packaging/systemd]
    tests: [phase2_dual_socket_boundary]
    evidence: [evidence/phase2/socket-authority-results.json]
    releaseGate: mandatory
  - id: INV-P2-SOCKET-002
    class: invariant
    statement: Gateway requests require the configured SO_PEERCRED process identity and exact Ed25519 request signature; local requests require UID 0 or genuine se-z group membership.
    enforcedBy: [src/kernel/identity.mjs]
    tests: [phase2_peer_authority]
    evidence: [evidence/phase2/socket-authority-results.json]
    releaseGate: mandatory
  - id: INV-P2-CATALOG-001
    class: invariant
    statement: The active 0.1A catalog contains only complete implemented operation definitions and its digest covers every interpretation-relevant property.
    enforcedBy: [src/kernel/definitions.mjs]
    tests: [phase2_catalog_complete]
    evidence: [evidence/phase2/catalog-results.json]
    releaseGate: mandatory
  - id: INV-P2-EXEC-001
    class: invariant
    statement: Exact argv and arbitrary shell execution default to unrestricted UID-0 host execution in /root with no timeout unless explicitly requested.
    enforcedBy: [src/kernel/jobs.mjs]
    tests: [phase2_raw_execution]
    evidence: [evidence/phase2/execution-results.json]
    releaseGate: mandatory
  - id: INV-P2-JOB-001
    class: invariant
    statement: Every execution has a durable job and process identity sufficient to survive supervisor restart and avoid PID-reuse adoption or cancellation.
    enforcedBy: [src/kernel/jobs.mjs, src/kernel/job-runner.mjs]
    tests: [phase2_durable_jobs]
    evidence: [evidence/phase2/job-results.json]
    releaseGate: mandatory
  - id: INV-P2-STREAM-001
    class: invariant
    statement: Stdout and stderr are separate binary-safe durable streams with deterministic paging and no total output cap.
    enforcedBy: [src/kernel/job-runner.mjs, src/kernel/jobs.mjs]
    tests: [phase2_large_binary_stream]
    evidence: [evidence/phase2/stream-results.json]
    releaseGate: mandatory
  - id: INV-P2-IDEMPOTENCY-001
    class: invariant
    statement: Semantic idempotency and nonce replay are durable, distinct, and serialized so concurrent identical mutations launch at most once.
    enforcedBy: [src/kernel/state.mjs]
    tests: [phase2_concurrent_idempotency]
    evidence: [evidence/phase2/idempotency-results.json]
    releaseGate: mandatory
  - id: INV-P2-RESUME-001
    class: invariant
    statement: Response loss is recoverable by original request ID or exact idempotency identity without duplicate mutation.
    enforcedBy: [src/kernel/state.mjs, src/kernel/operations.mjs]
    tests: [phase2_response_loss_resume]
    evidence: [evidence/phase2/resume-results.json]
    releaseGate: mandatory
  - id: INV-P2-FILE-001
    class: invariant
    statement: The complete 0.1A raw host file family is implemented with paged reads, identity-bound patching, and crash-durable atomic replacement.
    enforcedBy: [src/kernel/files.mjs]
    tests: [phase2_file_lifecycle]
    evidence: [evidence/phase2/file-results.json]
    releaseGate: mandatory
  - id: INV-P2-PTY-001
    class: invariant
    statement: PTY sessions have stable identities, exact offsets, persistent backing, restart rediscovery, input, resize, read, and honest close or loss states.
    enforcedBy: [src/kernel/pty.mjs]
    tests: [phase2_pty_lifecycle]
    evidence: [evidence/phase2/pty-results.json]
    releaseGate: mandatory
  - id: INV-P2-ARTIFACT-001
    class: invariant
    statement: Artifacts are binary-safe, resumable, offset-checked, digest-verified, atomically finalized, immutable, paged, and explicitly removable.
    enforcedBy: [src/kernel/artifacts.mjs]
    tests: [phase2_artifact_lifecycle]
    evidence: [evidence/phase2/artifact-results.json]
    releaseGate: mandatory
  - id: INV-P2-RECEIPT-001
    class: invariant
    statement: Ed25519 receipts bind request, result, generation, catalog, host, release, state, job, stream, and artifact digests without raw output.
    enforcedBy: [src/kernel/crypto.mjs, src/kernel/operations.mjs]
    tests: [phase2_receipt_tamper_and_rotation]
    evidence: [evidence/phase2/receipt-results.json]
    releaseGate: mandatory
  - id: INV-P2-CLI-001
    class: invariant
    statement: The native se-z CLI uses the local SEZ1 socket, waits deterministically, preserves binary output, resumes requests, and verifies receipts.
    enforcedBy: [src/bin/se-z.mjs]
    tests: [phase2_cli_acceptance]
    evidence: [evidence/phase2/execution-results.json]
    releaseGate: mandatory
  - id: INV-P2-RECONCILE-001
    class: invariant
    statement: Restart reconciliation uses durable state and actual operating-system process truth and reports lost or ambiguous rather than fabricated success.
    enforcedBy: [src/kernel/jobs.mjs, src/kernel/pty.mjs, src/kernel/artifacts.mjs]
    tests: [phase2_restart_reconciliation]
    evidence: [evidence/phase2/reconciliation-results.json]
    releaseGate: mandatory
  - id: INV-P2-STATE-001
    class: invariant
    statement: Correctness-bearing supervisor state uses atomic write, file flush, rename, and parent-directory flush under the canonical 0.1A ownership layout.
    enforcedBy: [src/kernel/util.mjs, src/kernel/state.mjs]
    tests: [phase2_state_durability_and_corruption]
    evidence: [evidence/phase2/reconciliation-results.json]
    releaseGate: mandatory
  - id: INV-P2-PACKAGE-001
    class: invariant
    statement: The immutable digest-addressed 0.1A candidate contains the exact supervisor, CLI, schemas, catalog, native addon, units, tmpfiles, notices, and manifest without private credentials or production state.
    enforcedBy: [scripts/phase2/package.mjs]
    tests: [phase2_package_reproducibility]
    evidence: [evidence/phase2/release-manifest.json]
    releaseGate: mandatory
  - id: INV-P2-HOST-001
    class: invariant
    statement: An isolated candidate proves signed UID-0 execution, durable streams, restart, file compatibility, and response-loss resume on the authorized VPS while preserving Baby protected configuration.
    enforcedBy: [scripts/phase2/host-candidate.sh]
    tests: [phase2_host_candidate_acceptance]
    evidence: [evidence/phase2/host-candidate-acceptance.json, evidence/phase2/production-readback.json]
    releaseGate: mandatory
  - id: ANTI-P2-FALLBACK-001
    class: anti-invariant
    statement: Explicit nspawn or KVM targets return target_not_supported and never fall back to host.
    enforcedBy: [src/kernel/jobs.mjs]
    tests: [phase2_unsupported_target_no_fallback]
    evidence: [evidence/phase2/execution-results.json]
    releaseGate: mandatory
  - id: ANTI-P2-OUTPUT-CAP-001
    class: anti-invariant
    statement: No inherited 64 MiB or other total output cap exists.
    enforcedBy: [src/kernel/job-runner.mjs]
    tests: [phase2_output_over_64mib]
    evidence: [evidence/phase2/stream-results.json]
    releaseGate: mandatory
  - id: ANTI-P2-REDACTION-001
    class: anti-invariant
    statement: Explicit owner-authorized raw command, file, PTY, and stream bytes are never altered by broad secret-pattern redaction.
    enforcedBy: [src/kernel/job-runner.mjs, src/kernel/files.mjs, src/kernel/pty.mjs]
    tests: [phase2_raw_output_byte_exact]
    evidence: [evidence/phase2/secret-scan.json]
    releaseGate: mandatory
  - id: ANTI-P2-INACTIVE-OP-001
    class: anti-invariant
    statement: Phase 3, Phase 4, and Phase 5 operation families are absent from the active 0.1A registry.
    enforcedBy: [src/kernel/definitions.mjs]
    tests: [phase2_inactive_operations_absent]
    evidence: [evidence/phase2/catalog-results.json]
    releaseGate: mandatory
  - id: ANTI-P2-PUBLIC-001
    class: anti-invariant
    statement: Phase 2 creates no TCP, HTTP, tunnel, public MCP, Caddy, or ChatGPT app listener or activation.
    enforcedBy: [src/kernel/server.mjs, packaging/systemd]
    tests: [phase2_no_public_listener]
    evidence: [evidence/phase2/production-readback.json]
    releaseGate: mandatory
  - id: ANTI-P2-SUCCESS-001
    class: anti-invariant
    statement: Accepted, running, lost, or ambiguous work is never represented as completed success.
    enforcedBy: [src/kernel/jobs.mjs, src/kernel/operations.mjs]
    tests: [phase2_running_not_success]
    evidence: [evidence/phase2/job-results.json]
    releaseGate: mandatory
  - id: ANTI-P2-PRODUCTION-001
    class: anti-invariant
    statement: Phase 2 candidate testing does not activate se-z production, change Baby protected configuration, or change the current ChatGPT app.
    enforcedBy: [scripts/phase2/host-candidate.sh]
    tests: [phase2_baby_before_after_readback]
    evidence: [evidence/phase2/production-readback.json]
    releaseGate: mandatory
  - id: ANTI-P2-THEATER-001
    class: anti-invariant
    statement: Phase 2 acceptance inspects implementation and real execution; evidence booleans are never the sole proof.
    enforcedBy: [scripts/phase2/verify.mjs]
    tests: [phase2_no_theater_audit]
    evidence: [evidence/phase2/no-theater-audit.json]
    releaseGate: mandatory

# Phase 3 stable gates
- id: INV-P3-MCP-001
  class: invariant
  statement: Phase 3 control enforced by integrated implementation and executable acceptance.
  enforcedBy: [src/phase3]
  tests: [test/phase3]
  evidence: [evidence/phase3]
  releaseGate: 0.1B
- id: INV-P3-OAUTH-001
  class: invariant
  statement: Phase 3 control enforced by integrated implementation and executable acceptance.
  enforcedBy: [src/phase3]
  tests: [test/phase3]
  evidence: [evidence/phase3]
  releaseGate: 0.1B
- id: INV-P3-RESOURCE-001
  class: invariant
  statement: Phase 3 control enforced by integrated implementation and executable acceptance.
  enforcedBy: [src/phase3]
  tests: [test/phase3]
  evidence: [evidence/phase3]
  releaseGate: 0.1B
- id: INV-P3-PKCE-001
  class: invariant
  statement: Phase 3 control enforced by integrated implementation and executable acceptance.
  enforcedBy: [src/phase3]
  tests: [test/phase3]
  evidence: [evidence/phase3]
  releaseGate: 0.1B
- id: INV-P3-OWNER-001
  class: invariant
  statement: Phase 3 control enforced by integrated implementation and executable acceptance.
  enforcedBy: [src/phase3]
  tests: [test/phase3]
  evidence: [evidence/phase3]
  releaseGate: 0.1B
- id: INV-P3-REFRESH-001
  class: invariant
  statement: Phase 3 control enforced by integrated implementation and executable acceptance.
  enforcedBy: [src/phase3]
  tests: [test/phase3]
  evidence: [evidence/phase3]
  releaseGate: 0.1B
- id: INV-P3-REVOCATION-001
  class: invariant
  statement: Phase 3 control enforced by integrated implementation and executable acceptance.
  enforcedBy: [src/phase3]
  tests: [test/phase3]
  evidence: [evidence/phase3]
  releaseGate: 0.1B
- id: INV-P3-GATEWAY-STATE-001
  class: invariant
  statement: Phase 3 control enforced by integrated implementation and executable acceptance.
  enforcedBy: [src/phase3]
  tests: [test/phase3]
  evidence: [evidence/phase3]
  releaseGate: 0.1B
- id: INV-P3-TUNNEL-001
  class: invariant
  statement: Phase 3 control enforced by integrated implementation and executable acceptance.
  enforcedBy: [src/phase3]
  tests: [test/phase3]
  evidence: [evidence/phase3]
  releaseGate: 0.1B
- id: INV-P3-GATEWAY-SIGNING-001
  class: invariant
  statement: Phase 3 control enforced by integrated implementation and executable acceptance.
  enforcedBy: [src/phase3]
  tests: [test/phase3]
  evidence: [evidence/phase3]
  releaseGate: 0.1B
- id: INV-P3-GITHUB-001
  class: invariant
  statement: Phase 3 control enforced by integrated implementation and executable acceptance.
  enforcedBy: [src/phase3]
  tests: [test/phase3]
  evidence: [evidence/phase3]
  releaseGate: 0.1B
- id: INV-P3-GITHUB-API-001
  class: invariant
  statement: Phase 3 control enforced by integrated implementation and executable acceptance.
  enforcedBy: [src/phase3]
  tests: [test/phase3]
  evidence: [evidence/phase3]
  releaseGate: 0.1B
- id: INV-P3-GITHUB-GIT-001
  class: invariant
  statement: Phase 3 control enforced by integrated implementation and executable acceptance.
  enforcedBy: [src/phase3]
  tests: [test/phase3]
  evidence: [evidence/phase3]
  releaseGate: 0.1B
- id: INV-P3-GITHUB-RECONCILE-001
  class: invariant
  statement: Phase 3 control enforced by integrated implementation and executable acceptance.
  enforcedBy: [src/phase3]
  tests: [test/phase3]
  evidence: [evidence/phase3]
  releaseGate: 0.1B
- id: INV-P3-RELEASE-001
  class: invariant
  statement: Phase 3 control enforced by integrated implementation and executable acceptance.
  enforcedBy: [src/phase3]
  tests: [test/phase3]
  evidence: [evidence/phase3]
  releaseGate: 0.1B
- id: INV-P3-RELEASE-ROLLBACK-001
  class: invariant
  statement: Phase 3 control enforced by integrated implementation and executable acceptance.
  enforcedBy: [src/phase3]
  tests: [test/phase3]
  evidence: [evidence/phase3]
  releaseGate: 0.1B
- id: INV-P3-RELEASE-REPAIR-001
  class: invariant
  statement: Phase 3 control enforced by integrated implementation and executable acceptance.
  enforcedBy: [src/phase3]
  tests: [test/phase3]
  evidence: [evidence/phase3]
  releaseGate: 0.1B
- id: INV-P3-TEMP-APP-001
  class: invariant
  statement: Phase 3 control enforced by integrated implementation and executable acceptance.
  enforcedBy: [src/phase3]
  tests: [test/phase3]
  evidence: [evidence/phase3]
  releaseGate: 0.1B
- id: INV-P3-PACKAGE-001
  class: invariant
  statement: Phase 3 control enforced by integrated implementation and executable acceptance.
  enforcedBy: [src/phase3]
  tests: [test/phase3]
  evidence: [evidence/phase3]
  releaseGate: 0.1B
- id: ANTI-P3-PUBLIC-MCP-001
  class: anti-invariant
  statement: Phase 3 control enforced by integrated implementation and executable acceptance.
  enforcedBy: [src/phase3]
  tests: [test/phase3]
  evidence: [evidence/phase3]
  releaseGate: 0.1B
- id: ANTI-P3-EXTRA-TOOL-001
  class: anti-invariant
  statement: Phase 3 control enforced by integrated implementation and executable acceptance.
  enforcedBy: [src/phase3]
  tests: [test/phase3]
  evidence: [evidence/phase3]
  releaseGate: 0.1B
- id: ANTI-P3-WRONG-AUDIENCE-001
  class: anti-invariant
  statement: Phase 3 control enforced by integrated implementation and executable acceptance.
  enforcedBy: [src/phase3]
  tests: [test/phase3]
  evidence: [evidence/phase3]
  releaseGate: 0.1B
- id: ANTI-P3-REFRESH-REPLAY-001
  class: anti-invariant
  statement: Phase 3 control enforced by integrated implementation and executable acceptance.
  enforcedBy: [src/phase3]
  tests: [test/phase3]
  evidence: [evidence/phase3]
  releaseGate: 0.1B
- id: ANTI-P3-TOKEN-PERSISTENCE-001
  class: anti-invariant
  statement: Phase 3 control enforced by integrated implementation and executable acceptance.
  enforcedBy: [src/phase3]
  tests: [test/phase3]
  evidence: [evidence/phase3]
  releaseGate: 0.1B
- id: ANTI-P3-GITHUB-TOKEN-LEAK-001
  class: anti-invariant
  statement: Phase 3 control enforced by integrated implementation and executable acceptance.
  enforcedBy: [src/phase3]
  tests: [test/phase3]
  evidence: [evidence/phase3]
  releaseGate: 0.1B
- id: ANTI-P3-DUAL-WRITER-001
  class: anti-invariant
  statement: Phase 3 control enforced by integrated implementation and executable acceptance.
  enforcedBy: [src/phase3]
  tests: [test/phase3]
  evidence: [evidence/phase3]
  releaseGate: 0.1B
- id: ANTI-P3-FAKE-FENCING-001
  class: anti-invariant
  statement: Phase 3 control enforced by integrated implementation and executable acceptance.
  enforcedBy: [src/phase3]
  tests: [test/phase3]
  evidence: [evidence/phase3]
  releaseGate: 0.1B
- id: ANTI-P3-PRODUCTION-CUTOVER-001
  class: anti-invariant
  statement: Phase 3 control enforced by integrated implementation and executable acceptance.
  enforcedBy: [src/phase3]
  tests: [test/phase3]
  evidence: [evidence/phase3]
  releaseGate: 0.1B
- id: ANTI-P3-BABY-MUTATION-001
  class: anti-invariant
  statement: Phase 3 control enforced by integrated implementation and executable acceptance.
  enforcedBy: [src/phase3]
  tests: [test/phase3]
  evidence: [evidence/phase3]
  releaseGate: 0.1B
- id: ANTI-P3-THEATER-001
  class: anti-invariant
  statement: Phase 3 control enforced by integrated implementation and executable acceptance.
  enforcedBy: [src/phase3]
  tests: [test/phase3]
  evidence: [evidence/phase3]
  releaseGate: 0.1B
- id: ANTI-P3-PARTIAL-COMPLETE-001
  class: anti-invariant
  statement: Phase 3 control enforced by integrated implementation and executable acceptance.
  enforcedBy: [src/phase3]
  tests: [test/phase3]
  evidence: [evidence/phase3]
  releaseGate: 0.1B
