import fs from 'node:fs';import {execFileSync} from 'node:child_process';const required=['contracts/operations-0.1b.json','contracts/errors-0.1b.json','contracts/gateway-state-schema-0.1b.json','packaging/phase3/systemd/se-z-gateway.service','packaging/phase3/systemd/se-z-tunnel.service','evidence/phase3/package-reproducibility.json','evidence/phase3/no-theater-audit.json'];const failures=[];for(const f of required)if(!fs.existsSync(f))failures.push(`missing ${f}`);const ops=JSON.parse(fs.readFileSync('contracts/operations-0.1b.json'));if(!ops.operations.some(o=>o.name==='sez.github.api')||!ops.operations.some(o=>o.name==='sez.release.repair'))failures.push('operations incomplete');const tool=JSON.parse(fs.readFileSync('contracts/tool-schema.json'));if(JSON.stringify(tool).includes('enum'))failures.push('public operation enum');const pkg=JSON.parse(fs.readFileSync('evidence/phase3/package-reproducibility.json'));if(!pkg.passed)failures.push('package not reproducible');execFileSync(process.execPath,['--test','test/phase3/phase3.test.mjs','test/phase3/http.test.mjs','test/phase3/github.test.mjs','test/phase3/failure.test.mjs'],{stdio:'inherit'});const out={passed:failures.length===0,failures,head:execFileSync('git',['rev-parse','HEAD'],{encoding:'utf8'}).trim(),tree:execFileSync('git',['rev-parse','HEAD^{tree}'],{encoding:'utf8'}).trim()};console.log(JSON.stringify(out));if(failures.length)process.exit(1);
