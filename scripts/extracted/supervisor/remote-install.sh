#!/usr/bin/env bash
# Provenance: exact pinned source and transformation are recorded in vendor/baby-provenance/file-map.json.
# Retired v1 SSH installer. Standalone v2 uses the Sez-owned fixed controller.
set -euo pipefail
echo "ERROR: remote installation is superseded by the standalone Sez deployment controller" >&2
exit 64
